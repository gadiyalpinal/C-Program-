#include <stdio.h> 
int main() 
{
    int grams;
    float kilograms; 
    printf("Enter the weight in grams: ");  
                                          
    scanf("%d", &grams);
    kilograms = grams / 1000; 
    printf("%d grams is equal to %f kilograms.", grams, kilograms);
}